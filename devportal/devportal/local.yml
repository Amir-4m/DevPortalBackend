version: '3'

volumes:
  devportal_local_postgres_data: {}
  devportal_local_postgres_data_backups: {}

services:
  django:
    build:
      context: .
      dockerfile: ./compose/local/django/Dockerfile
    image: devportal_local_django
    container_name: devportal_local_django
    depends_on:
      - postgres
    volumes:
      - .:/app:z
    env_file:
      - ./.envs/.local/.django
      - ./.envs/.local/.postgres
    ports:
      - "8000:8000"
    command: /start

  postgres:
    build:
      context: .
      dockerfile: ./compose/production/postgres/Dockerfile
    image: devportal_production_postgres
    container_name: devportal_local_postgres
    volumes:
      - devportal_local_postgres_data:/var/lib/postgresql/data
      - devportal_local_postgres_data_backups:/backups
    env_file:
      - ./.envs/.local/.postgres

  docs:
    image: devportal_local_docs
    container_name: devportal_local_docs
    build:
      context: .
      dockerfile: ./compose/local/docs/Dockerfile
    env_file:
      - ./.envs/.local/.django
    volumes:
      - ./docs:/docs:z
      - ./config:/app/config:z
      - ./devportal:/app/devportal:z
    ports:
      - "9000:9000"
    command: /start-docs
